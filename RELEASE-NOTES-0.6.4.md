# MingDao Harness v0.6.4 发布说明

> 状态：**待发布**（tag `v0.6.4`）。本版**不掺新功能**。
> 内容是 v0.6.1 三份第三方审计报告 + v0.6.2 自审报告的**收口修复**，共九批。
> v0.6.3 发布说明「已知边界」里列的那批条目，本版处理掉了大部分。

这一版修的东西有同一个形态：**没有报错，也没有崩溃，但防护/能力/数据实际上是空的**——
安全防护被绕过（fail-open）、红线配了不生效、合规事件缺字段、并发下悄悄丢一行、
被审代码在 CI 上被执行、同一份回答被计费两次。因此每一条都要求先复现、再修，
并且**用变异验证断言本身**（把修复改回去，断言必须失败；抓不到就说明断言是假绿）。

---

## 一、最该先看的三条（实测复现、影响面最大）

**① 凭证库损坏后会静默清空其余全部凭据。**
`credentials.json` 被截断一个字符后，再执行一次 `mingdao key set …`，
**其余凭据全部消失，而且打印的是成功**。根因是把「文件不存在」与「解析失败」都当成空对象，
而写路径是「读 → 改一条 → 全量重写」。
现已区分两者：**读不出来就拒绝写**（退非 0，原文件原样保留供人工抢救），
并容错 PowerShell 写的 BOM。

**② 会话原文不脱敏就上传云同步。**
自动同步默认开启，于是**你粘贴过的密钥会明文上传到同步服务**。
现在推送前统一过脱敏器（本地文件保持原样，远端与其它设备只拿掩码版）。
顺带发现脱敏器自己漏了三种最常见的凭据——**PEM 私钥块、JWT、赋值式**
（`AWS_SECRET_ACCESS_KEY=` 这类因字段名匹配要求过严而全部漏掉）。已补齐，
并加了「不误伤」的反向断言（普通中文、`max_tokens: 4096`、JSON 配置、`monkey=` 都不动）。

**③ `pack verify` 会执行被审代码。**
帮助文本写的是「只做静态校验」，实现却无条件 `import pack.mjs`。
下游 CI 把它当门禁用，等于**在 CI 上以完整 Node 权限执行被审仓库的任意代码**。
现在默认**静态**（manifest / 兼容窗口 / 声明文件 / 入口存在性，全程不 import），
要真执行必须显式 `--runtime` 且会先警告。

---

## 二、安全：fail-open 与攻击面（批二 / 批五）

防护「看起来在、实际被绕过」的四处，以及 Web 侧四个入口：

| 位置 | 原行为 | 现在 |
| --- | --- | --- |
| 预设「未提权」分支 | 把配置里的 `allow`/`deny` **整体丢弃**（`deny:['fetch:*']` 静默消失） | 对象进对象出，只改 `mode`，禁令保留 |
| hook 载荷写失败 | 空输出被当成「放行」→ 本该拦截的策略静默通过 | **fail-closed**：载荷没送到且 hook 无输出即按 hook 失败处理 |
| completeness 必填项 | 「缺失」哨兵只认中文「未提及」→ 英文会话里红线**因语言静默失效** | 多语种哨兵集合 + 约束可自声明 `missingValues`（刻意**不含** `无`/`none`：医疗域里「过敏史: 无」是有效数据） |
| 自建 registry | 允许明文 `http`（而索引 sha256 与正文同源自证，TLS 是唯一外部信任锚） | 非回环 http 与其它协议一律拒绝 |

- **符号链接逃逸（P0，实测复现）**：目录围栏原先只做路径前缀比较，而 `statSync`/`readdirSync`
  **跟随符号链接**——在家目录里 `ln -s /` 再登记成工作空间，Agent 的 bash/write/edit 就整体
  跑到了围栏外。改为按 **realpath** 判定，且目标与允许根**两边都归一化**
  （只归一化一边会误判合法目录，macOS `/var`→`/private/var` 就是实例）。
- **元数据端点无条件拒绝**：此前「回环绑定」会整段跳过 SSRF 检查，云元数据地址（169.254…）
  因此可达。现在无条件拒绝，`allowPrivate` 也不放行。刻意**不**拒绝 CGNAT 段——
  100.64/10 正是 Tailscale 地址段，拒绝它会打断真实的自托管用法。
- **跨站盲打**：无令牌 + 有副作用的 GET（含「读取即删除」的 `GET /api/draft`）可被
  外站的 `<img>`/no-cors 请求打中。现按 `Sec-Fetch-Site` 判定，cross-site / same-site 一律 403，
  Origin 校验扩展到所有方法。
- **自定义 Provider 名穿越**：`provider: '../evil'` 原本能 `import` 目录之外的模块。
  现为名字白名单 + 路径包含性检查，非法名退化为「没有自定义模块」。

## 三、合规能力：配了却没生效（批三）

v0.6.0 主打的账本 / 回放 / 领域约束，有四处**实际空转**：

- 约束事件**缺 `id`** → 账本与回放都读 `id`，于是「是哪条红线拦的」**恒为 null**；
- **`confirm` 从不求值**：它能过装载校验、会计入约束数量（`active=true`），却被跳过。
  现在权限放行之后仍要人工确认；**答否、或拿不到交互通道（`-p` / 调度 / 子 Agent）
  一律按阻断处理**（回放无交互通道时单列 `confirm-required`，不再静默算通过）；
- **CLI 的 `ledger replay` 恒假阴性**：命令分发发生在挂 Pack 之前 → 约束恒空 →
  它恒报「没有任何生效的领域约束」，当 CI 门禁时**恒通过**；
- `ledger replay --json` 因为布尔开关写在末尾取不到值，**恒输出人读文本**；
- 五条失败路径（非法 runId、找不到账本、未知子命令等）此前**退 0**，现在统一退 1。

## 四、静默数据损失（批四）

全部发生在并发或多进程场景，单进程手测看不到：

- **配置「读不出来」被当成「没有配置」**：改坏一个字符后跑 init 或桌面首启，
  `customModels`/`mcpServers`/`sync`/`net`/`costGuard` 会被全新对象**整文件覆盖且不备份**。
  现在先**改名备份**再写，并明确告警；
- **手动 `/compact` 用追加落盘**（会话文件近乎翻倍、`-c` 恢复后历史重复），
  而自动压缩走的是原子重写——统一到重写；
- **压缩后不清读取去重缓存**：正文已换成摘要，缓存仍说「你看过」→ 模型只拿到占位串；
- **费用明细追加在锁外**：A 的那一行会被 B 的读-改-写覆盖；追加与轮转收进同一把锁；
- **审计日志轮转吃行**（合规证据不能丢）：与上一条同法；Web 日志在请求路径上，
  改为**改名式轮转**（整文件 rename），没有读-改-写窗口因此不需要锁。

## 五、供应链与并发长驻（批六 / 批七）

- **未信任的项目级同名 Pack 能把内置版本静默顶掉**（而它自己又不挂载 = 两边都没生效）：
  现在冲突变成启动告警，遮蔽**必须可见**；
- **项目级预设按名静默遮蔽内置/用户级**（可注入 `systemPrompt`/`tools`）：打印双方路径告警；
- **git 安装器**加形态白名单（只收 `https`/`ssh`/`git`/`git@host:path`），
  `file://` 与本地路径明确拒绝；
- **同步服务的写锁没有 `await`** → 临界区异常变未处理拒绝 → **整个同步服务退出**；
- **锁外读快照、锁内整份写回** → 分享被 revoke 后并发 accept 会让它「复活」，
  改密时已吊销的 token 也会复活；
- **调度恢复判据恒真** → 任务永久卡 `running`；宿主死了但 worker 还活着时又会重排 →
  **同一任务跑两遍**；锁的陈旧回收只看 pid 存活，**pid 复用**会让锁僵死。

## 六、计费与可用性（批八）

- **非幂等重试导致重复计费**：`chat` 是 POST，上游一旦开始生成就按量计费，
  而重试只看「错误是否瞬态」——流式空闲超时也会重试 → **你看到一份回答，账本记两次**。
  现在收到过任何一帧就不再重试（代价：已开始输出后不再静默重试）；
- `compactTrigger` 只夹下限不夹上限 → **写成 >1 会让自动压缩永久失效**；
- 降级回合按 `modelName` 计价 → 与实际模型单价差数倍，「降级省钱」反而看不出来；
- 上游**提前关流**（无 `[DONE]`、无 `finish_reason`）现在可检出：界面提示 + 账本标记；
- content-type 被网关改写成 `application/json` 时按内容嗅探回退，**正文不再整段丢失**。

## 七、上游能力与文档（批九）

- skill 安装器的**临时目录清理只在一条早退路径上**（中途抛异常就留下整个目录）→ 收进 `finally`；
- **检查点文件名可 `../../` 穿越**到目录之外 → 加包含性检查，非法会话名如实返回失败；
- **`--auth-token` 写在命令行会进 argv / `ps` / shell 历史** → 支持 `--auth-token=-` 从 stdin 读，
  字面量给出明确告警与更安全的替代方式；
- 六处文档与实现漂移逐处对齐，并**加断言钉住文档**（文档回退即测试失败）。

---

## 升级须知（会影响下游的行为变更）

1. **`pack verify` 默认不再执行 Pack 代码。** 若你的 CI 依赖它执行 `pack.mjs`，
   需要显式加 `--runtime`（并清楚那是在 CI 上执行被审仓库的代码）。
2. **`confirm` 约束现在真的会拦人。** 此前「配了 confirm 却从不生效」的 Pack，
   升级后会开始要求人工确认；在 `-p`、调度任务、子 Agent 这些**没有交互通道**的路径上，
   命中即阻断——若你的自动化流程依赖这些 Pack，请先确认这是你要的行为。
3. **hook 判定改为 fail-closed**：载荷没能送到 hook 且 hook 无输出时，
   不再当「放行」，而是按 hook 失败处理。
4. **云同步上传的会话内容已脱敏**（本地文件不受影响，保持在原样）。
5. **私有文件权限收紧到 0600 并每次写自愈**（会话/记忆/任务/账本等 10 处），目录补 0700。
6. **自建 registry 的非回环 `http` 被拒**（回环仍允许，便于本地开发）。
7. **浏览器跨站请求一律 403**（同源/直接导航不受影响）。
8. **工作空间 API 返回 realpath 规范化后的路径**（围栏与返回值同口径）。
9. 新增 `denyStrict: true` 可把 `deny` 升级为**不可临时放行**的硬拦截（默认 `false`，行为不变）。

## 验证

- `smoke` **148 组断言**（v0.6.3 为 139）、`e2e-local` / `e2e-web` / `e2e-schedule` /
  `api-contracts` / `bench` 全通过；
- `tsc` **0 错误**、strict 棘轮 **0/0**；
- CI 五腿（ubuntu 18/20/22 + macOS + Windows）全绿——期间修掉两处**测试夹具**的
  平台假设（符号链接用例在 Windows 上目标不存在；源码级正则用 `\n` 写死被 CRLF 破坏）；
- 每一批都用**变异验证断言本身**：批一 7 / 批二 5 / 批三 9 / 批四 8 / 批五 8，
  其余批次逐条同类验证。被如实记录的失败情形包括 **3 处等价变异**（删掉那行断言照旧全绿，
  说明它本就不是防线）与若干处「断言第一版抓不到变异」（收紧后重验才算数）。

## 已知边界（如实说明）

- 仍有 **15 处**同步文件锁在极端争用下会阻塞其所在进程——已核实不在 WebUI 请求路径上
  （`schedule` 在守护进程内、`sync` 是 CLI 一次性命令、`cachestats` 仅在 >4MB 轮转时），
  且列成受审阅清单（新增即测试失败）。**不宣称"阻塞面已彻底消除"。**
- 账本**可选签名**（`--sign-key`）仍未实现：封条只提升到「防误删/漏写」，
  **不等同审计级不可否认**，这一点已写进导出物。
- `audit-report.md` 里其余约 15 项只有**代号**（明细需逐条复现），`B-CS-1` 记为
  「部分落实」——**不宣称穷尽**。
- 本版内容在开发期按 `v0.6.3/批一…批九` 标记（提交信息与部分源码注释仍是该标记），
  但 v0.6.3 已于 2026-09-15 发版，因此它们**实际随 v0.6.4 发布**；
  用户文档中的「v0.6.3 起」已按实际发布版本更正为「v0.6.4 起」。
