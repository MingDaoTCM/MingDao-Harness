# MingDao Harness v0.6.5 发布说明

> 状态：**待发布**（tag `v0.6.5`）。**补丁版，只做安全，不掺任何新功能、不改任何正常路径的用法。**
> 内容是独立审计（8 名审计员、58 项发现）里**唯三的越权／任意执行链**，
> 外加把这三条钉死的回归断言。其余发现按审计报告 §10 的优先级排队，**不在本版范围内**。
> 机制、复现与变异验证记录见 `docs/AUDIT-v0.6.1-第三方报告登记.md` §3.39。

这一版修的三条链有同一个形态：**功能全对，报错也没有，但"不该拿到的东西"能被拿走**——
写操作能跑出工作目录围栏、未信任的第三方代码能被执行、已存储的 API Key 能被骗到别人的端点。
因此每一条都按「**先复现 → 定级 → 修复 → 回归断言 → 变异验证断言本身**」推进，
并做了 **4/4 变异验证**（把每处修复逐个改回缺陷，断言必须当场失败）。

**建议所有用户升级**，尤其是：会 `clone` 别人仓库再用本工具打开的人（第 1、2 条），
以及在共享机器 / 共享 CI 上跑 WebUI 的人（第 3 条）。

---

## 一、悬空符号链接能把写操作送出工作目录围栏（严重）

**谁能中招**：任何"工作目录里存在一个指向外部的符号链接、而链接目标当前不存在"的场景——
clone 一个仓库、解一个 tar 包、装一个 npm 包都会带进来（README 明确把"clone 任意仓库"列为威胁模型）。
`auto` 档下**没有任何人工闸门**。

**原来会怎样**：`write` / `edit` / `undo` 会**跟随**这个链接，把内容写到工作目录之外，
可覆盖 `~/.ssh/authorized_keys`、`~/.zshrc`、`~/.mingdao/config.json`（后者还能再用来执行 hook）。
实测（审计报告 P0-1）：

```
写结果: {"ok":true,"output":"已写入 …/danglink（14 字节）。"}
界外文件是否被创建: true "ESCAPED-WRITE\n"
同一路径 read: {"ok":false,"error":"路径越界（工作目录外）…"}     ← 同一条路径读被拦、写放行
```

根因是边界判定用 `realpathSync` 归一，而它对**目标不存在**的链接会抛 `ENOENT`——
旧实现于是退回"只比较字符串路径"，判定**链接自己**（而不是它指向的地方）在围栏内。

**现在**：`realpathSync` 失败时不再"上跳一级猜"，改判 `lstatSync`；是符号链接就 `readlinkSync`
解析目标后**递归**判定，并加 16 层深度上限（链接成环**有界返回失败**，不会再无限解析）。
反向用例也被断言钉住：**悬空但指向围栏内**的链接照常可写，不会把"先建链接、后建目标"的正常用法一起挡掉。

## 二、`pack info` 会执行未信任 Pack 的代码（严重）

**谁能中招**：在别人给你的仓库目录里执行过 `mingdao pack info <名字>`（帮助文本主动推荐这个命令）。

**原来会怎样**：v0.6.2 修"clone 仓库 → 跑个子命令 → 任意代码执行"时，把"信任门"加在了
挂载路径上，`pack list` 也会正确显示「⛔ 未信任（不挂载）」并给出 trust 指引——
但 `pack info` 直接加载 Pack 模块，也就是 `import pack.mjs`。
于是"**换个子命令就能执行**"：未信任的第三方代码以**完整 Node 权限**在同一进程里跑，
可读 `~/.mingdao/credentials.json`、可出网。

**现在**：`pack info` 与挂载路径同口径，未信任就拒绝执行，并说明两件事——
**为什么**（完整 Node 权限）和**怎么办**（`mingdao pack trust <目录>`），退出码非 0。
断言同时钉住"显式 `trust` 之后必须恢复可用"（门挂在信任状态上，不是"永远拒绝 info"）
以及"信任后内容一变即失效"的既有语义。

## 三、改 API 地址＝把已存储的 Key 交给谁（高危）

**谁能中招**：在共享机器 / 共享 CI 上跑 WebUI 且未设访问令牌（默认的"本机信任模式"）——
同机任意进程都能调用 `/api/*`。

**原来会怎样**：`setBaseUrl`（"API 地址覆盖"）只校验地址格式，**不看凭证**，
于是三个请求就能把**已存储的完整 API Key** 骗到攻击者端点（审计报告 P1-20，实测）：

```
① 改 baseUrl 指向攻击者端点: {"ok":true,"baseUrl":"http://127.0.0.1:50322/v1"}
② 清 provider 缓存        : {"ok":true,…}
③ 攻击者端点收到 Authorization = Bearer sk-victim-FULL-SECRET-ABCDEFGHIJKLMNOP
```

这一条**不是"少了个确认框"**：`GET /api/models-config` 只回脱敏 Key（`sk-vic…3456`）、
凭证文件是 0600、`~/.mingdao` 是 0700 —— 这条链把三层保护一起绕过。
而且改动是持久的：受害者之后**每一次对话**都继续把 Key 发给攻击者。

**现在**：把"改出网目的地"当**凭证级操作**——该服务商**已有存储 Key** 且本次改动会改变目的地
`origin` 时，必须在同一请求里重新提交那个 Key（SHA-256 摘要比对），否则返回 `{needKey:true}`；
WebUI 收到后用隐藏输入提示补输并自动重试。判据是"**目的地变了**"，不是"字符串变了"：
同 origin 只换路径（本机模型 `/v1` → `/v2`）、以及该服务商本来就没有 Key 的情况**完全不受影响**。

**顺带收口的同类第二个入口**（修复期间新发现，主审复现）：WebUI 的「修改」已有自定义模型
（`updateCustom`）同样会把该模型已存 Key 发往新地址——实测攻击者端点收到
`Bearer sk-victim-CUSTOM-KEY-…`。它现在走同一个凭证级校验。
这条的回归断言是**行为级**的：先断言正常端点**真的收到了** Key（证明这条链平时是通的，
而不是"因为链坏了所以没外带"），再断言改向被拒后攻击者端点**0 次访问**。

---

## 升级须知（会影响下游的行为变更）

1. **未信任的项目级 Pack：`pack info` 不再执行其代码。** 若你的脚本依赖它输出贡献面明细，
   先执行一次 `mingdao pack trust <目录>`（内容一变需重新 trust）。
2. **WebUI 里改"API 地址"会要求重输该服务商的 API Key**（仅当该服务商已有存储 Key 且目的地 origin
   变了时）。同 origin 换路径、以及给没有 Key 的服务商配地址，行为不变。
3. 其余行为**完全不变**：没有改任何工具、权限、计费、账本或配置格式。

## 验证

- 新增 `test/smoke.js` §123（220 行断言）：四条修复 + 反例保护（悬空但指向围栏内的链接、
  同 origin 换路径、无 Key 服务商）+ 环状链接有界返回 + 行为级外带链路；
- **变异验证 4/4**：把每处修复逐个改回缺陷，断言当场失败（其中"悬空软链"变异直接复现出
  原始缺陷的 `ok:true` 写到围栏外）；
- 全门禁绿：`tsc` 0 错误、strict 棘轮 0/0、`smoke` **151 组断言** / `e2e-local` / `e2e-web` /
  `e2e-schedule` / `api-contracts` / `bench` 全通过；CI 五腿（ubuntu 18/20/22 + macOS + Windows）全绿；
- 诚实标注层级：UI 交互本身（补输 Key 的弹窗）在无浏览器环境下只能做**源码级**断言，
  已在断言里写明；行为级部分用的是真实 WebUI + 真实凭据文件 + 打桩端点。

## 已知边界（如实说明）

- 本版**只做**上述三条链。审计报告的其余发现（计费/护栏异常路径、并发读写丢数据、
  脱敏器的引号键、截断与轮转、测试可信度等）**仍在**，按报告 §10 的优先级在后续版本处理。
- 同类但**另一个机制**的越权仍在：`workspace.js` 的"符号链接注册后改指向击穿围栏"（报告 P1-21）。
  它不在本次指定的三条链内，已列入下一批第一项——**不宣称"越权面已清空"**。
- 判据用的是"**生效中的 Key**"（环境变量 > 凭证库 > `config.json` 的 `apiKey` 字段），
  也就是说：即使你的 Key 来自环境变量（`DEEPSEEK_API_KEY` 等）或写在 `config.json` 里，
  只要它非空、且这次改动会改变目的地 `origin`，就会要求确认——确认方式是把同一个 Key 值再输一遍
  （自动化脚本请在同一请求里带上 `apiKey` 字段）。反过来，该服务商**没有任何可用 Key** 时不拦截：
  没有可被外带的秘密，就不该拿可用性去换一个空防线。
