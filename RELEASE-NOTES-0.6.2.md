# MingDao Harness v0.6.2 发布说明

> 状态：**待发布**（tag `v0.6.2`）。本轮为**审计欠账清算版**，共 26 批修复、42 个提交，
> 无新增功能面，但其中两处属**高危**，建议所有用户升级。

## 本轮最要紧的两处（均为实测复现，不是推测）

### 一、安装技能可能清空整个 `~/.mingdao`（凭据、会话、账本一起没）

安装线上技能时，拼路径用的是**索引里的名字**，而做校验的是**下载文件里自己声明的名字**
——两个独立输入，可以不一致。入口那道字符集检查看着像白名单，**却允许 `.` 与 `..`**：

| 索引里写 | 后果 |
| --- | --- |
| `{"name": "."}` | 安装目录被解析成**整个 skills 目录** → 你已安装的**全部技能被递归删除** |
| `{"name": ".."}` | 解析成**整个 `~/.mingdao`** → 配置、**API 凭据**、会话历史、执行账本**全部清空** |

而且两种情况下**界面都显示安装成功**。技能索引来自网络（自建 registry / 镜像），
且索引里的名字不在任何哈希保护范围内（每个文件的 sha256 是**对着索引**校验的，索引本身不签名）。

已修：入口先校验索引名、安装目录改用技能自己声明（且已校验）的名字、底层再加一道兜底。

### 二、执行账本"尾部被截断"查不出来

`mingdao ledger verify` 此前只比对链内相邻哈希，而**删掉尾部若干行**（包括 `run.end`）后
链内完全自洽——实测「只留前 3 行」照样报"完整"。尾部截断恰恰是最常见的形状
（写入中断、误删、随手截断）。

已修：每次回合收尾额外写一份**封条**（应有条数 + 链头哈希）。现在能区分
「链内一致」与「完整」：条数少了报截断、多了报追加、末行不符报尾部改写；
**没有封条时不再谎报完整**，而是明确说"无法确认"。

> ⚠ **行为变更**：`mingdao ledger verify` 对"链内一致但完整性未知"现在**退出码为 1**
> （以前会显示 ✅）。脚本里若依赖该退出码，请留意。

## 一类系统性问题的清算：写入失败却报告成功

对用户数据而言这比"少一个文件"严重得多——**你被告知成功了**。本轮**穷举后**逐个处理：

- **守护进程 pidfile** 写失败 → 每次调用都**再拉起一个守护进程** → 同一批定时任务并发执行多次
  （正是原来那段锁注释要防的问题被静默重新引进门）；
- **任务检查点**写失败 → 跑满步数时提示「发送『继续』即可」，可续跑摘要却永远不存在；
- **工作空间**写失败 → 照样打印「✓ 已登记」，实际列表里永远不会有它；
- **费用明细**写失败 → 日费用护栏少计，**可能超支而不自知**；
- **项目记忆去重**写失败 → 仍上报「已去重 N 条」；**审计日志**写失败 → 完全无声。

关键不是"这次仔细点"，而是**把穷举固化成测试**：全仓扫描"静默吞掉写操作"的位置，
与一份**已审阅白名单**比对（每处写明为什么可以吞）。新增即测试失败。

## 其余修复（摘要）

- **安全**：约束正则的灾难性回溯（实测 `(a+)+$` 对 29 字符要 **4.8 秒**，
  一个 Pack 就能让内核卡死）→ 装载期拒绝并说明原因；SSRF 逐跳复检收成单一来源；
  出网闸门不再静默把请求降级成 GET；`deny` 规则按命令段匹配（`a && b` 不再只查前半句）。
- **崩溃**：`mingdao … | head -1` 这类最常见的管道用法此前会**带堆栈崩溃**（stdout EPIPE 未处理），
  现已安静退出；账本/文件锁不再有"15 秒死区"。
- **静默失效**：词表一旦读失败就**终生**退化成估算（误差可达 ±2 倍）且无声 → 改为有界重试并说清后果；
  日志轮转改为按文件大小触发（此前对命令行用户是死代码，文件会无限增长）。
- **注入面**：项目记忆注入围栏加固；终端控制序列统一过滤（防转义序列伪造输出）。
- **发布链路凭据**：镜像发布脚本此前把 token 当**命令行参数**传给服务器、还把明文**回显**出来，
  gitee 的 token 又写在 URL 里 → 改为经 ssh stdin + 读后即删 + HTTP 头鉴权。

## 四平台发布纪律固化

「必须同时发 GitHub + Gitee + GitCode + npm」这句口头约定，现在由
`scripts/verify-release.mjs` 在发版最后一步**自动逐项校验**（git 引用、Release、npm 版本与 dist-tags），
任一渠道缺失即退出非 0——**口头约定记不住，脚本才记得住**。

## 升级方式

```bash
npm i -g mingdao-harness@latest     # 或
curl -fsSL https://harness.mingdao.ai/install.sh -o install.sh && bash install.sh
```

桌面版：安装包见下方附件，或在应用内检查更新。官网下载页：https://harness.mingdao.ai/#downloads

## 已知边界（如实说明，避免误解）

- `mingdao ledger` 的完整性校验**不含可信时间戳**，封条与账本同权限同目录——
  能防误删/漏写/随手改，**防不住同时改写两者的对手**，不等同于审计级不可否认。
- 文件锁仍是**同步**实现：它已不会误抢活锁、也没有死区，但在极端争用下仍会阻塞事件循环。
  异步化需要迁移 24 个调用点，本轮**未做**（已登记）。
- 三份第三方报告中仍有少数条目因**未给出代码位置**而无法核实（如 `B-CMD-4`），
  以及一份报告的明细文件未提供（约 15 项只有代号）。这些**没有**被当作已修复。
