# MingDao Harness v0.6.6 发布说明

> 状态：**待发布**（tag `v0.6.6`）。**补丁版，只做正确性，零新功能、零行为变更。**
> 内容来自第三方对 v0.6.5 的技术评估报告（§4.1–§4.3）：三处"**CI 五腿全绿，但在标准
> Windows 账户上跑不起来**"的问题——门禁在真机上其实是空的。逐条复现与机制核实见
> `docs/AUDIT-v0.6.1-第三方报告登记.md` §3.40，变异验证 **7/7**。

**只升级运行时**的用户：本版**不改任何产品行为**（调度、工具、权限、计费、账本、配置格式全不动）。
本版真正修的是**测试与门禁**——影响的是"改动是否被验证过"，而不是"软件怎么跑"。

不过有一条**运行时**改动值得单独说，见下面第二节：**守护进程被接管后的退出延迟**。

---

## 一、Windows 上"绿"是假的：三处门禁在真机上从未真正运行

### ① bench 的 214 条断言在 Windows 上从不执行

`npm test`（`node test/run-all.mjs`）是全部套件的汇总入口。在 Windows 上它**直接崩溃**：

```
Error: spawn EINVAL        (test/run-all.mjs)
```

根因是 Node ≥18.20.2 / 20.12.2（CVE-2024-27980 那批安全修复）起，
**spawn `.cmd`/`.bat` 而不开 `shell` 会同步抛 `EINVAL`**。而"同步抛"意味着 ChildProcess
根本没被创建——已经写好的 `child.on('error')` 永远收不到，Promise 直接 reject，整个汇总器崩溃，
**前面套件的失败详情与汇总表一起丢失**。

后果不是"少跑一个测试"：**bench 五套（214 条断言，包括"省钱幅度只降不升"的基准回归）
在 Windows 上从来没跑过**。CI 之所以一直是绿的，是因为 run-all 的唯一自动化入口
`npm run coverage` 只在 **Linux + Node 20** 那条腿上执行——Windows 腿碰不到它。

现在：Windows 上给 spawn 加 `shell: true`，并把同步启动路径包进 `try/catch`
（那时记为"该套件失败"，而不是让汇总器崩掉）。这个失败模式只在 Windows 出现
（POSIX 上 spawn 一个不存在的 `.cmd` 是异步 `ENOENT`，不是同步抛错），
所以本机无法做行为级复现——我们补的是**源码级守卫**，并在测试里**如实标注了层级**，
没有假装它是行为级验证。

### ② 软链用例把"没抛错"当成"建出了链接"

`smoke` 里有一批"符号链接逃逸应被拒绝"的用例，写法是：

```js
try { fs.symlinkSync(target, link); } catch { /* 跳过 */ }
```

这段代码**只判"抛不抛错"，不判"到底有没有建出链接"**。在未开 Developer Mode / 非管理员的
Windows 上（Node 通常抛 `EPERM`；某些受限或沙箱文件系统甚至会**静默把建链请求降级成一个普通空文件**），
于是用例实际测的是"**读/写一个普通文件**"——断言必然失败或必然通过，但**测的不是围栏**。
而 CI runner 以管理员运行、能建真链接，所以长期是绿的。

现在：新增 `makeRealSymlink()` 能力探测——建链后**必须用 `lstat().isSymbolicLink()` 复核**，
并替换了**全部 6 处**软链用例（第三方报告只点到 2 处）。建不成就在输出里显式打印
「⚠ 跳过」，**绝不假装测过**；另加反向自检：测试文件里再出现裸 `fs.symlinkSync` 就判失败。

### ③ 围栏判据不再依赖"机器有没有开开发者模式"

安全边界不能只在"能建软链的机器上"被验证。本次把 `withinRoot()` 导出并支持注入 fs 实现
（`withinRoot(root, target, depth, io = fs)`），新增一组**平台无关**的桩 fs 单测：

| 场景 | 期望 |
| --- | --- |
| 悬空链接指向围栏**外** | 拒绝（这正是 v0.6.5 P0-1 的漏洞形态） |
| 悬空链接指向围栏**内** | 放行（不能退化成"凡悬空一律拒绝"） |
| 链接成环 | 有界拒绝（断言 `readlink` 调用 ≤20 次，防栈溢出/挂死） |
| 目标不存在且不是链接（＝写新文件） | 放行（合法路径） |
| 根自身 / 围栏内子目录 | 放行 |

另外还**测了探测器自己**：静默降级（不抛错但没建出链接）必须判为失败、真建成必须判为成功、
`EPERM` 必须判为失败——否则"探测器失灵"本身没人发现（这一点在开发中被变异验证抓出来过一次）。

## 二、守护进程被接管后，最坏 60s 才退出（运行时行为改进）

调度守护进程（`mingdao schedule`）靠 pidfile 租约防止双跑。被新 daemon 接管后，旧 daemon
需要退出——但 `every` 型任务的等待是**一整段** `min(max(nextRunAt−now, 1s), 60s)` 的 sleep，
**等待期间不查租约**。

实测（`--every 60s` 任务 + 等 sleeper 进入等待片后篡改租约）：**旧 daemon >40s 仍存活**；
修复后同一探针 **4.0s 退出**。

虽然不会造成双跑（唤醒后才检查租约，若已失去就直接退出），但这 60s 窗口里新旧 daemon 会
交错操作同一批调度状态。现在 `every` 与 `once`（同一处缺陷的第二个入口，第三方报告只点了 `every`）
都改为 **≤3s 的切片等待、逐片复查租约**：最坏接管延迟 **60s → ≈5s**（监督轮询 2s + 一片 3s），
语义完全不变（睡够才继续）。

这也修掉了 `e2e-schedule` 租约用例在标准 Windows 账户上的确定性失败——它只等 20s。
用例另外补了 **5s 预热**（原来"篡改太早"会让用例偶然通过、从而掩盖缺陷）并把延迟钉在 ≤12s。

---

## 升级须知

1. **产品行为只有一处变化**：`schedule` 守护进程在租约丢失后最多 ~5s 退出（原最多 60s）。
   正常调度、周期语义、避峰等待都不变。
2. 其余改动全在 `test/`、文档与一处 `withinRoot()` 的**导出**（对外 API 无破坏性变化）。
3. 若你的 CI 直接用 `node test/smoke.js` 等单套命令，行为不变；用 `npm test` 的（Windows 上）
   从现在起才会真正跑完 6 套并产出汇总表。

## 验证

- **变异验证 7/7**：把每处修复逐个改回缺陷（围栏递归解析、能力探测判据、裸 `symlinkSync` 回归、
  run-all 的 shell、run-all 的 `try/catch`、调度切片（源码级）、调度切片（行为级）），
  断言全部当场失败；
- 全门禁绿：`tsc` 0 错误、strict 棘轮 0/0、`smoke` **153 组断言** / `e2e-local` / `e2e-web` /
  `e2e-schedule` / `api-contracts` / `bench` 全通过；
- **CI 五腿全绿（含 Windows）**——本次新增的源码级守卫是跨平台的（已按 CRLF 形态写过一遍，
  本仓曾两次栽在"文本断言写死 `\n`"上）；
- 如实标注：`run-all` 的 EINVAL 只在 Windows 出现，本机（macOS）无法行为级复现，
  因此那一处是**源码级**守卫；其余均为行为级。

## 已知边界（如实说明）

- 本版**只做**上述三处。第三方评估报告里其余建议按 §7 排期：
  **P0-2** 在线缓存命中基准（需要真实 Key 跑 `prompt_cache_hit/miss`，用于裁决"删 token 是否反而
  破坏缓存"——目前 bench 里"回收不额外劣化"是推理而非实测）、**P1-1** `agent.js` 拆分
  （`runTurn` ~1000 行）、**P1-2** 结构化摘要 + 关键事实钉死、**P1-3** 预算状态注入上下文、
  **P1-4** 出网闸门对"bash 里 curl"的补强、**P1-5** `docs/ARCHITECTURE.md` 重写
  （该文档确实停留在 v0.1.x 口径：全文 0 次提到 Pack/约束/账本/出网/预设/本地自适应，报告的批评成立）。
- 本版**不含** v0.6.5 独立审计报告里那 55 项未修条目（按该报告 §10 优先级排队），
  也**不含**同类但不同机制的 `workspace.js` 符号链接"注册后改指向"（报告 P1-21）。
- 标准 Windows 账户上，软链用例现在是**显式跳过**而非真验证；真正的覆盖由桩 fs 单测（§124）
  承担——这是有意的取舍，写在这里免得被读成"Windows 上全测过了"。
