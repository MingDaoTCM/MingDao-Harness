# MingDao Harness v0.6.1 发布说明

> 状态：**已发布**（2026-09-12，tag `v0.6.1`）。补丁版，内容只有修复。

## 修了什么

### 厂家给模型改名后，新模型选不了、也计不了费（本轮主因）

DeepSeek 官方把 **`deepseek-v4-flash` 改名为 `deepseek-flash`**
（依据：`GET /v1/models` 现在只返回 `deepseek-flash` 与 `deepseek-v4-pro`）。
而内核里**两处不同步**——设置界面用动态拉取的名单（能正确显示新名字），
切换模型的接口只认内置静态表（判「未知模型」）——于是**应用自己列出来的模型选不了**：

> 实测报障：设置里刷新能拉到 2 个模型，聊天界面选 `deepseek-flash` 报
> 「未知模型 deepseek-flash」并弹回 `deepseek-v4-pro`。

两处都修了：

1. **切换接口接纳「动态发现的模型」**：判据用服务商 `/models` 的返回——仍是有界集合
   （已按 chat 模型过滤、上限 200），因此「拦任意字符串」的护栏依然有效，
   而厂家改名/上新这类正常演进立刻可用，**不必等内核发版**。
2. **`deepseek-flash` 补成一等公民**：上下文上限与**价格**显式给出
   （沿用 `deepseek-v4-flash` 的数值——同一个模型换了名字）。
   这点同样重要：只修选择而不给价格，费用会退化成「无法估算」。
   **旧名 `deepseek-v4-flash` 保留**（老配置与历史会话里写的就是它，删掉会直接 404），
   仅在标签里标明「官方已改名，建议切换」；服务商回退名单也改成与官方 `/v1/models` 一致。

### 其他（工程侧，不影响使用）

- **CI 的 `ubuntu-22` 偶发失败**：e2e-web 挑端口的区间（40000–60000）落在 Linux 的
  临时端口范围（32768–60999）内，而这个测试自己要发大量 HTTP 请求，内核把同一端口
  分给了测试**自己的出站连接** → `EADDRINUSE`。改为在 20000–32000 内挑并先 bind 实测。
- **macOS 在线更新的 feed**：桌面版读的是官网 `/updates/`，而那里曾长期指向 **dmg**；
  electron-updater 在 macOS 上必须用 **zip**。已在官网发布流程侧修正为 zip
  （并补上 feed 生成器，避免再次手写出错）——**这条对已装 0.6.0 的用户立即生效，不需要等本版**。

## 验证

- 真实调用 `deepseek-flash` → HTTP 200，返回 usage；
- 费用可算：100 万输入 + 10 万输出闲时 ≈ ¥1.95（不再「无法估算」）；
- 切换接口：`deepseek-flash` → 200；**只在动态名单里的假想新名 → 200**；`12345` → 400（护栏仍在）；
- 回归断言 smoke **102** 组；三项变异校验全部抓住；CI 五腿绿。

## 明确不做

- 不改任何既有模型的名字或价格（`deepseek-v4-flash` 只是被标注为旧名，仍可用）；
- 不引入新依赖（零依赖不变）。
