# MingDao Harness v0.6.0 发布说明

> 状态：**已发布**（2026-09-12，tag `v0.6.0`）。
>
> 主题：**合规与确定性（确定性③：行为确定性）** —— 把「每步可审计、可回放、可自证」做成内核能力，
> 让私有化 / 受监管场景能自己回答「这个结论是怎么来的、花了多少钱、数据有没有出门」。
>
> 落地计划与逐项进度见 [docs/PLAN-v0.6.0.md](docs/PLAN-v0.6.0.md)；
> 本版同时收口了 v0.4.6 审计登记表的**全部仓库内条目**（v0.5.1 已按决策取消，
> 因此 v0.4.7 修复系列随本版一起发布），完整清单见 [docs/AUDIT-v0.4.6.md](docs/AUDIT-v0.4.6.md)。

---

## 为什么是这三件事（而不是继续堆能力）

头部厂商在拼**能力上限**：更强的模型、更多的工具、更大的生态。我们追不平，也不该追。
私有化 / 受监管场景真正卡住采购的是另外三件事，而云平台的多租户托管模型**架构上给不了**：

| # | 确定性 | 交付版本 |
| --- | --- | --- |
| ① | **约束确定性**——领域红线由内核强制，不是提示词里的一句话 | v0.5.0 |
| ② | **成本确定性**——每一分钱可归因、可预测、可拦截 | v0.5.0 |
| ③ | **行为确定性**——每步可审计、可回放、可自证 | **v0.6.0（本版）** |

---

## 新增能力

### 1. 执行账本（确定性③之一）

一次回合 = 一个账本文件（`~/.mingdao/ledger/<runId>.jsonl`），统一记录**八类事件**：
回合开始 / 模型轮次 / 工具调用 / 工具结果 / 约束触发 / 权限决策 / 费用 / 回合结束。

```bash
mingdao ledger list                      # 最近若干次运行
mingdao ledger show <runId>              # 人读明细
mingdao ledger export --format md --out 报告.md   # 脱敏导出（可交第三方复核）
mingdao ledger verify <runId>            # 哈希链校验：能发现改行**与删行**
```

- **不改造 `audit.jsonl`**：后者有 20000 行低频截断（回放会缺段，而被静默截断的合规账本比不记账更糟），
  且只记工具调用。账本独立成目录，`mingdao audit` 行为完全不变。
- **三条不可让步的纪律**：**写入即脱敏**（明细先过密钥规则再落盘，不留「先存明文靠导出兜底」的口子）；
  **摘要替代原文**（`argsDigest`/`resultDigest` = sha256 前 16 位，可比对、可防篡改、不泄露内容）；
  **约束事件不回显命中原文**（否则账本自身成为泄露渠道）。
- 导出时在密钥规则之上再叠加**私网 IP + 家目录**掩码（导出物是对外的）。
- 无价模型落 `priced:false` 与「无法估算」，**绝不写 `¥0.0000`** 冒充免费。
- **诚实边界**：哈希链只能证明「自写入后未被改动」，**不含可信时间戳**，不等同于审计级不可否认
  （可选 Ed25519 签名 `--sign-key` **尚未实现**，已在计划文档显式登记，请勿依赖）。

### 2. 决策回放（确定性③之二）

```bash
mingdao ledger replay <runId> [--json]   # 按**当前**权限/约束栈重判历史调用
```

输出四类差异：`now-blocked`（当时放行、今天会被红线拦住）/ `now-denied` /
`still-blocked` / `relaxed`。`now-blocked > 0` 时退出码为 1，**可直接当 CI 门禁**——
「新加的红线能不能拦住历史上那批操作」由此变成一条可断言的规则。

- **只承诺「决策回放」，不承诺「同一模型重放同一路径」**：模型本身非确定性，且要求离线环境
  持有同一权重；承诺了也做不到，不如不承诺。
- `ask` **不等于** `deny`：回放如实报 `ask`，不替用户回答（有专门断言钉住）。
- 局限如实写进输出：无生效约束时明说「本次只能证明这一点」（否则「0 条被拦」会被误读成
  「历史操作都合规」）；回放基于**脱敏后**参数；权限档位不同会提示，避免把档位变化误读成规则变化。

### 3. 出网白名单与自证（确定性③之三）

```json
{ "net": { "allow": ["api.deepseek.com", "*.internal.corp", "10.0.0.0/8"], "mode": "warn" } }
```
```bash
mingdao net policy      # 当前策略与是否生效
mingdao net report      # 本机访问过哪些外部地址（次数 / 放行 / 拦截 / 命中哪条规则）
```

- 出口**只在一处收口**（包 `globalThis.fetch`），而不是逐个改调用点——出网点分散在
  Provider / fetch 工具 / 技能库 / 模型发现 / 定价 / Batch，逐个改既易漏也会随时间漂移。
- **逐跳判定重定向**：默认跟随 3xx 时 undici 会自己跳，闸门只看得到首个 URL——
  这会被利用成「该主机 302 到任意地址，内核照样跟过去，并把 `Authorization` 头一起带过去」。
  现已改为自行跟随并逐跳判定，非白名单跳转**在发出请求前**即中止。
- 记账**只记主机/端口/判定/命中规则，不记请求体、不记完整 URL**。
- 回环（`localhost` / `127.0.0.0/8` / `::1`）始终豁免——本地模型是主力场景；**私网不豁免**，需显式列入。
- **未配置 `net` 时闸门完全不安装**，既有行为零影响。
- **诚实边界**：只覆盖**内核自己发起**的请求。用户在 `bash` 里自己敲的 `curl` 走子进程网络栈，
  不经过这里——它证明的是「内核没有偷偷外传」，**不是**「这台机器绝对没有外传」。

### 4. 内网 / 信创部署

```bash
bash install.sh --offline        # Linux / macOS
```
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File install.ps1 -Offline   # Windows
```

两侧对齐，均为**完全断网**安装：不下载 Node、不走 npm（`npm install -g .` 可能因 devDependencies
访问 registry），改用软链 / `.cmd` 包装直接调用源码（本项目运行时零依赖）。
离线包由 `bash scripts/build-offline-bundle.sh` 生成，含内网操作说明与校验值。
已用「屏蔽 curl/wget/git/npm + 代理指向黑洞」实测通过。

新增 **`vllm` / `ollama` / `oneapi`** 预设，覆盖国产推理栈（昇腾 MindIE、海光 DCU、寒武纪等
以 vLLM 为后端并暴露 OpenAI 兼容端点的栈）与内网聚合网关；
**本机/内网端点免除「必须有 API Key」的硬校验**（这类端点通常不校验也不发密钥），
公网端点仍必须有 Key；无 Key 时不再发送空的 `Authorization` 头（部分网关会因此 401）。

**诚实登记**：离线模式**不支持 `mingdao update` 自更新**（没有 `.git`），升级需重新投放离线包；
离线包**不含 Node 运行时**，需内网预装 Node ≥ 18.17。

---

## 修复（含随本版发布的 v0.4.7 修复系列）

v0.5.1 已按决策取消，因此 v0.4.6 审计登记表的**全部仓库内条目**随本版收口
（逐项 `file:line`、复现证据与变异校验见 [docs/AUDIT-v0.4.6.md](docs/AUDIT-v0.4.6.md)）。
这里只列影响面最大的几类：

**调度与任务生命周期**（v0.4.7）
- **重复 daemon → 同一调度任务被并发执行两次**（跨进程锁内完成「查活→spawn→写 pidfile」认领）；
- **pause/remove 停不住在途运行**（在跑的任务 id 立刻落盘，暂停/删除能真正杀掉在途 worker）；
- **僵尸任务回收**：worker 被 SIGKILL/OOM 杀死后任务永久停在 running、调度器**空转到 2 小时**上限；
- **终态写不再复活用户已 `kill` 的任务**（用户的停止是显式意图）；
- **跨平台 PID 归属校验**：此前只读 `/proc`，macOS 上恒为 null → 归属校验静默失效、
  PID 复用即误杀无关进程。

**约束引擎（合规特性的 fail-open，本版重点）**
- **`arg-forbid` 的正则写错时红线静默失效**（求值失败→永不命中，且不进 `invalid`，作者毫无察觉）
  ——与本模块自述的 fail-closed 原则直接矛盾；现为「装载即拒绝 + 运行期 fail-closed」；
- `result-forbid` 在契约里列着、实现却缺席（下游照契约写会被判 kind 非法而整包装载失败），已补实现；
- 约束 kind 集合此前有两份副本并已漂移，现统一为引擎单一来源。

**成本与计费**
- **Batch 中止不落到服务端**：用户按 Ctrl+C 后本地停了、服务端批次照跑照结算（0.5× 但全量 token）
  ——「停了」只是本地幻觉；现中止与 24h 超时都请求服务端取消，失败则如实提示仍可能计费；
- `sync-state` 丢更新（并发同步互相覆盖记账 → 反复全量传输甚至误判冲突）。

**安全与边界**
- 工作空间登记闸门、技能完整性诚实边界（来源不可验证的项目级技能）、
  API Key 不再经 argv 传递（否则会出现在 `ps` 里）；
- `install.ps1` 的 Node 版本判断只看 major → 18.0–18.16 误判合格（与 POSIX 侧同类缺陷）。

---

## 验证

- `node test/run-all.mjs` → **6/6 套通过**（smoke **101 组断言**）
- `npm run typecheck` → 0 错误；`scripts/strict-ratchet.mjs` → 0 / 0
- `npm run bench` → 214 断言全绿
- CI：Ubuntu 18/20/22 + **Windows** + macOS 五腿
  - 候选提交上 Ubuntu 20/22、**Windows**、macOS 四条腿已绿；Ubuntu 18 当时仍在排队
    （该 runner 近期排队较慢），其**前四个提交**上均为绿；
  - Windows 腿在 v0.6.0 开发期间曾**连续 5 个提交变红而未被发现**——根因是跨平台断言的
    能力探测判据用错（GitHub 的 Windows runner 自带 Git Bash，于是「有 bash」被误当成
    「POSIX 安装器适用」，而 `install.sh` 自己会拒绝 Windows）。已修，并把
    「每次 push 后必须查 CI」写成发版清单硬规则，见 AUDIT T32。
- 关键行为均做**变异校验**（还原缺陷 → 断言确实失败），本轮新增：账本脱敏（含嵌套）、
  哈希链篡改、T19 丢更新、T20 三项、出网重定向逐跳、Batch 服务端取消、
  `box()` 宽度、帮助单一来源、install.ps1 版本比较。

---

## 明确不做（本版）

- ❌ **模型级确定性回放**（见 §2：非确定性 + 需离线同一权重，做不到就不承诺）
- ❌ **审计级不可否认签名**（无可信时间戳；只能证明「自签署后未改动」）
- ❌ **全流量 DLP**（出网闸门只覆盖内核发起的请求，见 §3 诚实边界）
- ❌ **可选 Ed25519 签名 `--sign-key`**（计划 C1.7，未实现）
- ❌ `require-citation` 约束 kind 与 `mingdao constraint test`（见 `PACK-API.md §4.1` 已知缺口）
- ❌ 不引入任何重依赖（**零依赖不变**）

## 上游尚未提供（下游请勿依赖）

`ctx.storage`（Pack 私有持久化）· Pack 贡献非 OpenAI 兼容 Provider · Pack 私有存储加密 ·
账本的可选签名。前三项当前用 `<home>/providers/` 与 `permissions.fs` 显式路径替代。

## 下载

- 官网（国内直连极速）：https://harness.mingdao.ai/#downloads
- 桌面版安装包（Windows / macOS / Linux）：GitHub Release **长期保留**，不再清理
- npm：`npx mingdao-harness`（**发布后** latest 将取到本版；当前 npm latest 为 0.5.0）
