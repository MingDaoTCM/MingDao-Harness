# MingDao Harness v0.6.3 发布说明

> 状态：**待发布**（tag `v0.6.3`）。本版**不掺新功能**，只修下游与桌面版实际被卡住的三件事。
> 三项都是「本来该能用却用不了」，而且用户看不出原因——所以优先发出来。

## 一、域内任务拿不到工具（下游实测：「回访」用不了）

**现象**：`请生成回访看板` ✅ 会调用工具；而 `回访` ❌ 不会。上游为省 token 有个"只读档"优化：
回合开始只暴露只读工具，命中**写意图关键词**才给全量。于是：

- 域内任务用的是**开集**动词/名词（回访、排班、盘点、对账……），**永远枚举不完**；
- 更糟的是"释放条件"也等不到——工具看不见时，模型只会回一段文字，**根本不会进第二轮**。

**已修**（不做关键词堆砌）：

1. **判定方向反转**：只有"看起来是纯提问 **且** 无写意图"才进只读档，其余一律给全量工具；
2. **域内 Pack 在场时永不进只读档**（这类部署要的是"任务能做"）；
3. **自愈**：只读档若以纯文本收尾且透出"做不到/需要…"，放开工具集重试一次。

> 代价：非提问的普通消息现在也走全量档，那部分轮次的输入 token 会上升——这是刻意取舍
> （能力 > 省 token）。`config.schemaTier=false` 仍可整体关掉这一优化。

## 二、桌面版 bash 输出乱码

两处**独立**根因，都实测确认：

| 根因 | 机制 |
| --- | --- |
| 逐块解码 | 采集输出时把数据块**逐块**转字符串：中文 3 字节被管道切成两半，两半各自解出 `�` |
| Windows 代码页 | Windows 上 bash 工具走 `cmd.exe`，其输出是 OEM 代码页（中文 = GBK/936），按 UTF-8 解就是花屏 |

**已修**：改为按字节累积、结束时**一次**解码；严格 UTF-8 失败时用 **GBK** 再解一次。
顺带修掉同类问题：按字节裁剪会把 UTF-8 从字符中间切断（那会让解码误走 GBK 反而花屏），
现已对齐字符边界；超长截断也不再劈开 emoji 等代理对。

## 三、vision 门控不认自定义 Provider（下游 Dify 工作流已支持视觉）

**现象**：给上游发图发不出去。原门控只看内置预设与 `customModels`，于是**自定义 Provider
（Dify/网关适配器）永远被判不支持图片**；而为了让门控通过去写 `customModels`，
又会把 provider 解析**劫持**到 OpenAI 兼容直连——**自定义 Provider 模块被整个绕开**，
请求发去了 `baseUrl` 而不是适配器。

一句话：**「打开一个能力开关」不该改变「请求发给谁」**。

**已修（两条都做，覆盖两种表达方式）**：

```jsonc
// 路 A：只加能力声明 —— 不再劫持 provider
{ "provider": "dify", "customModels": { "<你的模型名>": { "vision": true } } }
```
```js
// 路 B：在 ~/.mingdao/providers/dify.mjs 里声明（推荐：能力与 transport 同处一个文件）
export const supportsVision = true;   // 或 export const capabilities = { vision: true }
export async function createProvider(cfg) { /* 你原本的实现，不变 */ }
```

判定顺序：**显式声明 > 内置预设 > Provider 静态声明 > 保守 false**。只读静态导出、
不调用 `createProvider()`（后者可能有副作用：起进程/建连接），按文件 mtime 缓存。
附件被拒时的提示现在会给出这三条正确做法；`docs/PROVIDERS.md` 已补说明与对照表。

## 工程侧收尾（不影响使用）

- **文件锁的阻塞面**：新增异步锁（等待时让出事件循环），WebUI 请求路径上的工作空间操作已迁移，
  请求不再冻结事件循环；临界区里不再做外部进程调用；同步锁等待上限依实测从 20 秒收到 **5 秒**
  （实测最坏临界区 6ms）。残留的 15 处同步锁已列受审阅清单，新增即测试失败。
- **发布校验脚本**：瞬时网络失败不再误报「四平台未对齐」（三次有界重试，并区分
  「取不到」与「真的不一致」）；发版清单里那条会把 token 写进 ssh 命令行的示例已改正。

## 验证

- `smoke` **139 组断言**、`e2e-local` / `e2e-web` / `e2e-schedule` / `api-contracts` / `bench` 全通过；
  `tsc` 0 错误、strict 棘轮 0/0；
- 三项修复共 **10 个突变**全部被预期断言捕获（判定方向 / Pack 旁路 / 逐块解码 / 边界对齐 /
  GBK 回退 / 能力覆盖劫持 / 门控不咨询 Provider / 不读显式声明 / `isCustom` 恒 false /
  WebUI 退回旧判定）；
- CI 五腿（ubuntu 18/20/22 + macOS + Windows）全绿。

## 已知边界（如实说明）

- 文件锁仍有 **15 处**在极端争用下会阻塞其所在进程——已核实不在 WebUI 请求路径上
  （`schedule` 在守护进程内、`sync` 是 CLI 一次性命令、`cachestats` 仅在 >4MB 轮转时），
  且已列成受审阅清单。**不宣称"阻塞面已彻底消除"。**
- 历史三份第三方报告 + 一份 v0.6.2 自审报告合计约 **60 条**待处理条目
  （凭据暴露面、fail-open、并发与长驻稳定性、供应链信任链等），**本版未包含**，
  正在按优先级分批推进；每批都会带回归断言与变异验证。
